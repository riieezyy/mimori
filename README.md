# mimori
A mobile-first offline study/productivity PWA.

## Run
Serve this folder over HTTP (GitHub Pages works). Open `index.html` through a local/static server; service workers require HTTPS or localhost.

## Data
Core app data is stored in IndexedDB in the browser. Uploaded images/PDF data are stored locally in the browser.
